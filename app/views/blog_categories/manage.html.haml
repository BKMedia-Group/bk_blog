%h1 Manage Blog Categories
%p= link_to 'New Category', new_blog_category_path, class: 'btn btn-primary'
%table.table.table-bordered.table-striped
  %thead
    %tr
      %th Name
      %th Published
      %th
  %tbody= render collection: @categories, partial: 'manage'